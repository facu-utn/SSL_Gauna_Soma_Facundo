#include <stdio.h> //.h stands for header file
int main ()
    {
        printf  ("Hello Dear World!\n");
        return 0;        
    }

//Para compilar escribo en terminal gcc nombreDelArchivo.c y se crea el ejecutable.
/*Si deseo el ejecutable con otro nombre uso el comando -o asi:
gcc nombreDelArchivo.c -o nombreDelEjecutable.exe 😁  */

//Para ejecutar ./nombreDeEjecutable.exe

//A diferencia de C++, C utiliza la función print f (), instead of cout <<

//To insert a new line, you can use the \n character